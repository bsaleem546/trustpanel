# Build outputs
bin/
obj/
out/
publish/

# .NET tooling
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
project.lock.json
artifacts/

# Test results & coverage
TestResults/
coverage/
*.trx
*.coverage
*.coveragexml
coverage*.json
coverage*.xml
coverage*.info

# Local secrets & config overrides
# Put real secrets here — these files are never committed:
#   appsettings.Local.json
#   appsettings.Development.Local.json  (merges on top of Development)
appsettings.Local.json
appsettings.*.Local.json
secrets.json

# ASP.NET Core Data Protection keys (generated at runtime, machine-specific)
keys/
**/keys/DataProtection-Keys/

# EF Core
*.edmx.diagram

# Docker local overrides & volumes
docker-compose.override.yml
.data/

# Rider / ReSharper
.idea/
*.DotSettings.user

# StyleCop / analyzers cache
StyleCopReport.xml
*.stylecop
